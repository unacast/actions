#!/bin/sh

BASEDIR=$(dirname "$0")

echo "Executing $* and creating status for error if not exit code 0"
sh -c "$*"
RESULT=$?
if [[ 0 != "${RESULT}" ]];then
    ${BASEDIR}/deployment-create-status error
    exit ${RESULT}
fi